
export type User = {
  name: string,
  email: string,
  imageUrl: string,
}

export type UserOption = {
    name: string;
    href: string ;
}

export type NavigationOption = {
  name: string, 
  href: string, 
  current: boolean
}
